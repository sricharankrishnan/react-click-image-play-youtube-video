/**
 * created to ensure that we do not have dependency cycle issue after using eslint to check quality
 * of code
 * */
interface FrameProps {
  embedLink: string;
  embedTitle: string;
}
export default FrameProps;
